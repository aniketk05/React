import React from 'react'

export default function JsxRule4() {
    //! in jsx we can't use attribute like "class" and "for" while wrtting the jsx instead of this we use "className" and "htmlFor" 
  return (
    <div>
        JsxRule4 

<div>

        <label htmlFor=""></label>
        <p className="para1">in jsx we can't use attribute like "class" and "for" while wrtting the jsx instead of this we use "className" and "htmlFor"</p>
</div>
      
    </div>
  )
}
